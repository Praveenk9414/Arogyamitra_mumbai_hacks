# Core web UIs
gradio>=3.40.0
streamlit>=1.20.0

# PDF/text extraction and OCR helpers
pymupdf>=1.22.0
pdfplumber>=0.8.0

# RAG & embeddings
chromadb>=0.4.3
nomic>=0.3.0

# Ollama python client (if using python API)
ollama>=0.1.0

# Sentence Transformers and reranking
sentence-transformers>=2.2.2
cross-encoder>=0.3.6

# LangChain-like lightweight pieces used (core doc model)
langchain-core>=0.0.28
langchain-text-splitters>=0.3.0

# Utilities
python-multipart>=0.0.5
pillow>=9.5.0
tqdm>=4.65.0

# Optional (audio transcription)
whisperx>=1.3.0  # optional; if you plan to transcribe audio locally
torchaudio>=2.2.0  # required by some speech libs; optional

# LLM wrapper fallback (only if you also want OpenAI support)
openai>=1.0.0

# For local testing and packaging convenience
pytest>=7.0.0

# Pin a minimum to avoid wildly old versions; feel free to relax or pin to exact versions for reproducible builds.
